{
  "mongo": {
    "host": "${DB_HOST}",
    "port": "${DB_PORT}",
    "database": "${DB_DATABASE}",
    "password": "${DB_PASSWORD}",
    "name": "mongo",
    "user": "${DB_USER}",
    "connector": "mongodb"
  }
}
